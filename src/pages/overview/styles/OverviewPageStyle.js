import styled from "styled-components";
import { Image, Card } from "semantic-ui-react";

export const ImageModalContainer = styled(Image)`
  width: 90vh;
  max-height: 90vh;
  margin: auto;
`;
export const CardGroup = styled(Card.Group)`
  justify-content: right;
`;
